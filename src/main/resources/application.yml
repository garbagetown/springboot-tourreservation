logging.level:
  garbagetown: DEBUG
  org.terasoluna: DEBUG

spring:
  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:file:/tmp/tourreservation;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
  thymeleaf.cache: false
  messages.basename: i18n/application-messages

security.basic.enabled: false

customer:
  initialBirthYear: 1980
  initialBirthMonth: 1
  initialBirthDay: 1
